const canvasWidth = 800;
const canvasHeight = 600;
const aircraftWidth = 50;
const aircraftHeight = 30;
const birdWidth = 40;
const birdHeight = 20;
const parachuteWidth = 30;
const parachuteHeight = 30;
const starWidth = 20;
const starHeight = 20;

export {
  canvasHeight,
  canvasWidth,
  aircraftHeight,
  aircraftWidth,
  birdHeight,
  birdWidth,
  parachuteHeight,
  parachuteWidth,
  starHeight,
  starWidth,
};
